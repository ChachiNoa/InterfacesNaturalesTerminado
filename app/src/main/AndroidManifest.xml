<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permisos requeridos por las funcionalidades -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Declara que la app usa la cámara, pero no es un requisito estricto -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InterfacesNaturales">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activities para los 4 botones del menú -->
        <activity
            android:name=".comprarvoz.MainComprarVoz"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".lenguajesenas.MainSenas"
            android:exported="false" />
        <activity
            android:name=".leertexto.MainLeerTexto"
            android:exported="false" />
        <activity
            android:name=".llamarmalla.MainMallaFacial"
            android:exported="false" />
        <activity
            android:name=".comprarvoz.ProductActivity"
            android:exported="false" />

    </application>

</manifest>